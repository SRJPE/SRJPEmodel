% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/storage.R
\name{get_many_model_fits}
\alias{get_many_model_fits}
\title{Retrieve Multiple Model Fit Objects}
\usage{
get_many_model_fits(
  con,
  model_name,
  sites = NULL,
  run_years = NULL,
  storage_account = "jpemodelresults",
  container_name = "model-results",
  access_key = Sys.getenv("AZ_CONTAINER_ACCESS_KEY")
)
}
\arguments{
\item{con}{A database connection object (e.g. from \code{\link[DBI:dbConnect]{DBI::dbConnect()}}).}

\item{model_name}{The consolidated DB model name to retrieve. Must be one of
\code{.approved_model_names} (e.g. \code{"abundance"}, \code{"pcap_one_site"}). The
specific model variant used for each fit is stored in the \code{model_type}
column of \code{model_run} and is not used for filtering here.}

\item{sites}{Optional character vector of sites to include (e.g.
\code{c("ubc", "lcc")}). When \code{NULL} all sites are returned.}

\item{run_years}{Optional integer vector of run years to include (e.g.
\code{2020:2024}). When \code{NULL} all run years are returned.}

\item{storage_account}{Azure storage account name. Defaults to
\code{"jpemodelresults"}.}

\item{container_name}{Azure blob container name. Defaults to
\code{"model-results"}.}

\item{access_key}{Azure storage access key with \strong{read} permissions.
Defaults to the \code{AZ_CONTAINER_ACCESS_KEY} environment variable.}
}
\value{
A named list of model fit objects. Names are formatted as
\code{"<site>_<run_year>"} (e.g. \code{"ubc_2020"}, \code{"lcc_2021"}). Any site ×
run_year that fails to download is returned as \code{NULL} with a warning
rather than aborting the whole batch.
}
\description{
Downloads the most recent model fit for each site × run_year combination,
querying the database to identify which runs to fetch and then pulling each
\code{.rds} from Azure Blob Storage. Results are returned as a named list keyed
by \code{"<site>_<run_year>"}.

All filter arguments are optional and combinable. With no filters the
function returns the most recent fit for every site × run_year combination
present in \code{model_run} for the given \code{model_name}.
}
\examples{
\dontrun{
# All abundance fits for every site × run_year
fits <- get_many_model_fits(con, model_name = "abundance")

# Filter to specific sites or run years
fits <- get_many_model_fits(con, model_name = "abundance",
                            sites = c("ubc", "lcc", "mill creek"))

fits <- get_many_model_fits(con, model_name = "abundance",
                            run_years = 2020:2024)

# Access a single result from the list
fits[["ubc_2022"]]
}
}
