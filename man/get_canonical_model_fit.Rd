% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/canonical.R
\name{get_canonical_model_fit}
\alias{get_canonical_model_fit}
\title{Retrieve the Canonical Model Fit}
\usage{
get_canonical_model_fit(
  con,
  results_name,
  site = NULL,
  run_year = NULL,
  site_selection = NULL,
  storage_account = "jpemodelresults",
  container_name = "model-results",
  access_key = Sys.getenv("AZ_CONTAINER_ACCESS_KEY")
)
}
\arguments{
\item{con}{A database connection object.}

\item{results_name}{The consolidated model name (e.g. \code{"bt-spas-x"},
\code{"pcap_one_site"}). Must be one of \code{.approved_model_names}.}

\item{site}{Optional. Site, for bt-spas-x scopes.}

\item{run_year}{Optional. Run year, for bt-spas-x scopes.}

\item{site_selection}{Optional. Site selection, for pcap_one_site scopes.}

\item{storage_account}{Azure storage account name. Defaults to
\code{"jpemodelresults"}.}

\item{container_name}{Azure blob container name. Defaults to
\code{"model-results"}.}

\item{access_key}{Azure storage access key with \strong{read} permissions.
Defaults to the \code{AZ_CONTAINER_ACCESS_KEY} environment variable.}
}
\value{
The model fit object (class \code{stanfit} or \code{bugs}).
}
\description{
Downloads the current canonical fit for a model type + scope, resolved via
\code{\link[=list_canonical_model_runs]{list_canonical_model_runs()}} rather than "most recent version" — use this
instead of \code{\link[=get_model_fit]{get_model_fit()}} wherever "the production run" is wanted
rather than "whatever was uploaded last".
}
\examples{
\dontrun{
get_canonical_model_fit(con, "bt-spas-x", site = "ubc", run_year = 2024)
}
}
