% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_pCap_site_year}
\alias{plot_pCap_site_year}
\title{Plot weekly abundance and capture probability for a site–year combination}
\usage{
plot_pCap_site_year(
  site,
  run_year,
  pcap,
  pCap_inputs = NULL,
  abundance,
  min_pCap_mult = 0.5,
  Nscale = 0.001,
  lb = 0.025,
  ub = 0.975
)
}
\arguments{
\item{site}{Character. Site name (e.g. \code{"ucc"}, \code{"knights landing"}).}

\item{run_year}{Numeric. Run year to plot.}

\item{pcap}{A fitted Stan model object for the pCap model appropriate for
\code{site}.  The model type is inferred automatically:
\code{"one_site"} (skew) for mainstem sites (\code{"tisdale"}, \code{"knights landing"}),
\code{"all_sites"} for all other sites.}

\item{pCap_inputs}{Optional. Pre-computed output of \code{prepare_pCap_inputs()}
for the \code{all_sites} model.  If \code{NULL} (default) and \code{site} is not a
mainstem site, the function calls \code{prepare_pCap_inputs()} internally.}

\item{abundance}{A fitted BUGS abundance model object (from
\code{fit_abundance_model_BUGS()} or loaded from \code{.Rdata}).}

\item{min_pCap_mult}{Numeric. Multiplier passed to \code{prepare_abundance_inputs()}.
Default \code{0.5}.}

\item{Nscale}{Numeric. Abundance scaling factor (result displayed in units of
\code{1 / Nscale} fish). Default \code{0.001} (thousands of fish).}

\item{lb}{Numeric. Lower quantile bound. Default \code{0.025}.}

\item{ub}{Numeric. Upper quantile bound. Default \code{0.975}.}
}
\value{
A named list with two ggplot objects:
\describe{
\item{\code{abundance_plot}}{Weekly abundance.}
\item{\code{pcap_plot}}{Weekly capture probability.}
}
}
\description{
Produces two ggplot panels for a given site and run year:
\enumerate{
\item Weekly estimated abundance (bars) with 95 \% CI, Lincoln-Peterson point
estimates, discharge overlay, and catch labels.
\item Weekly estimated capture probability (bars) with 95 \% CI, observed
efficiency trials, and discharge overlay.
}
}
