% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/canonical.R
\name{.canonical_fit_keys}
\alias{.canonical_fit_keys}
\title{Builds a result-list key per row of a \code{list_canonical_model_runs()} tibble,
based on whichever scope columns are actually populated for that row (they
vary by model type): \code{"<site>_<run_year>"} for bt-spas-x, \code{"<site>"} for
site-only scopes (e.g. plad_btspasx_results), \code{"<site_selection>"} for
pcap_one_site, and \code{model_name} itself when no scope column is populated
(e.g. pcap_all_sites).}
\usage{
.canonical_fit_keys(canonical, model_name)
}
\description{
Builds a result-list key per row of a \code{list_canonical_model_runs()} tibble,
based on whichever scope columns are actually populated for that row (they
vary by model type): \code{"<site>_<run_year>"} for bt-spas-x, \code{"<site>"} for
site-only scopes (e.g. plad_btspasx_results), \code{"<site_selection>"} for
pcap_one_site, and \code{model_name} itself when no scope column is populated
(e.g. pcap_all_sites).
}
\keyword{internal}
