% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_year_re_with_effort}
\alias{plot_year_re_with_effort}
\title{Plot year random effects and site-year capture probabilities}
\usage{
plot_year_re_with_effort(
  pcap,
  model_type = c("all_sites", "one_site"),
  site_selection = NULL,
  use_pdev = FALSE,
  use_effort = FALSE,
  lb = 0.025,
  ub = 0.975
)
}
\arguments{
\item{pcap}{A fitted Stan model object for the pCap model.}

\item{model_type}{Character.  Either \code{"all_sites"} or \code{"one_site"}.}

\item{site_selection}{Character. Required when \code{model_type = "one_site"}.
Site name passed to \code{prepare_pCap_inputs()}.}

\item{use_pdev}{Logical. Include process error when computing predicted
pCap? Default \code{FALSE}.}

\item{use_effort}{Logical. Include effort adjustment when computing
predicted pCap? Default \code{FALSE}.}

\item{lb}{Numeric. Lower quantile bound. Default \code{0.025}.}

\item{ub}{Numeric. Upper quantile bound. Default \code{0.975}.}
}
\value{
A named list with two ggplot objects:
\describe{
\item{\code{yr_re_plot}}{All year random effects.}
\item{\code{site_year_plot}}{Per-site pCap by year (faceted, or single panel
for \code{one_site}).}
}
}
\description{
Produces two ggplot objects from a fitted pCap model:
\enumerate{
\item All year random effects on a single panel, colour-coded by site.
\item Per-site panels showing estimated pCap by year, plus a "no efficiency
trial" (no_et) reference point in red.
}
}
