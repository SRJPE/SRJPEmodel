% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/juvenile_abundance.R
\name{extract_pCap_estimates}
\alias{extract_pCap_estimates}
\title{Extract parameter estimates from the pCap Stan model object}
\usage{
extract_pCap_estimates(model_object, pCap_inputs)
}
\arguments{
\item{model_object}{A \code{stanfit} object produced by \code{fit_pCap_model()}.}

\item{pCap_inputs}{A named list produced by \code{prepare_pCap_inputs()},
containing at minimum \code{sites_fit} and \code{model_name}.}
}
\value{
A tidy data frame with one row per parameter–statistic
combination, containing the following columns:
\itemize{
\item \strong{model_name} Character. The pCap model name
(e.g. \code{"pCap_all_sites"}, \code{"pCap_one_site_skew"}).
\item \strong{site} Character. The site name associated with the parameter, or
\code{NA} for hyper-parameters not tied to a specific site.
\item \strong{stream} Character. The stream associated with the site, or \code{NA}.
\item \strong{run_year} Always \code{NA} for the pCap model (pCap is estimated
across all years).
\item \strong{week_fit} Always \code{NA} for the pCap model (no weekly parameters
are extracted).
\item \strong{parameter} Character. The parameter name with index notation
removed (e.g. \code{"b0_pCap"}, \code{"b_flow"}, \code{"logit_pCap"}).
\item \strong{statistic} Character. The posterior summary statistic
(e.g. \code{"mean"}, \code{"sd"}, \code{"2.5"}, \code{"50"}, \code{"97.5"}, \code{"Rhat"}).
\item \strong{value} Numeric. The value of the statistic.
\item \strong{srjpedata_version} Character. The version of the \code{SRJPEdata} package
used when producing these estimates.
}
}
\description{
Extract parameter estimates from the pCap Stan model object
}
\details{
Tidies the summary table from a fitted \code{stanfit} pCap model into
a long data frame, attaches site and stream name lookups, and records the
\code{SRJPEdata} package version. For \code{one_site} (mainstem skew) models, the
site name is taken from \code{pCap_inputs$site} rather than the parameter
index. The resulting table is in the same format as
\code{extract_abundance_estimates()}.
}
