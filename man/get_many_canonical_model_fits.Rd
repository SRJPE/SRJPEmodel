% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/canonical.R
\name{get_many_canonical_model_fits}
\alias{get_many_canonical_model_fits}
\title{Retrieve Multiple Canonical Model Fit Objects}
\usage{
get_many_canonical_model_fits(
  con,
  model_name,
  sites = NULL,
  run_years = NULL,
  site_selections = NULL,
  storage_account = "jpemodelresults",
  container_name = "model-results",
  access_key = Sys.getenv("AZ_CONTAINER_ACCESS_KEY")
)
}
\arguments{
\item{con}{A database connection object (e.g. from \code{\link[DBI:dbConnect]{DBI::dbConnect()}}).}

\item{model_name}{The consolidated DB model name to retrieve. Must be one of
\code{.approved_model_names} (e.g. \code{"bt-spas-x"}, \code{"pcap_one_site"}).}

\item{sites}{Optional character vector of sites to include (e.g.
\code{c("ubc", "lcc")}). Applies to site-scoped model types (bt-spas-x,
plad_btspasx_results). When \code{NULL} all sites are returned.}

\item{run_years}{Optional integer vector of run years to include (e.g.
\code{2020:2024}). Applies to bt-spas-x. When \code{NULL} all run years are
returned.}

\item{site_selections}{Optional character vector of \code{site_selection}
values to include (e.g. \code{c("tisdale", "knights landing")}). Applies to
pcap_one_site. When \code{NULL} all site selections are returned.}

\item{storage_account}{Azure storage account name. Defaults to
\code{"jpemodelresults"}.}

\item{container_name}{Azure blob container name. Defaults to
\code{"model-results"}.}

\item{access_key}{Azure storage access key with \strong{read} permissions.
Defaults to the \code{AZ_CONTAINER_ACCESS_KEY} environment variable.}
}
\value{
A named list of model fit objects. Names depend on the scope of
\code{model_name}: \code{"<site>_<run_year>"} for bt-spas-x (e.g. \code{"ubc_2020"}),
\code{"<site>"} for site-only scopes like plad_btspasx_results,
\code{"<site_selection>"} for pcap_one_site (e.g. \code{"tisdale"}), or
\code{model_name} itself for unscoped model types like pcap_all_sites. Any
scope that fails to download is returned as \code{NULL} with a warning rather
than aborting the whole batch.
}
\description{
Downloads the current canonical fit for every scope matching \code{model_name}
(and any supplied filters), resolved via \code{\link[=list_canonical_model_runs]{list_canonical_model_runs()}}
rather than "most recent version" — the batch analog of
\code{\link[=get_canonical_model_fit]{get_canonical_model_fit()}}. Unlike \code{\link[=get_many_model_fits]{get_many_model_fits()}} (which is
specific to the site × run_year scope of abundance models), this works
across all scope shapes used in \code{canonical_model_run}:
\itemize{
\item \strong{bt-spas-x / plad_btspasx_results} — scoped by site × run_year (PLAD
fits omit run_year and are scoped by site alone).
\item \strong{pcap_one_site} — scoped by \code{site_selection} (e.g. \code{"tisdale"},
\code{"knights landing"}).
\item \strong{pcap_all_sites / other unscoped model types} — a single canonical fit
with no site/run_year/site_selection scope.
}

A single call only spans scopes for one \code{model_name} at a time — e.g. call
once with \code{"bt-spas-x"} and once with \code{"pcap_one_site"} to get both
families.
}
\examples{
\dontrun{
# All canonical abundance (BT-SPAS-X) fits for every site × run_year
fits <- get_many_canonical_model_fits(con, model_name = "bt-spas-x")

# Filter to specific sites or run years
fits <- get_many_canonical_model_fits(con, model_name = "bt-spas-x",
                                      sites = c("ubc", "lcc", "mill creek"))

fits <- get_many_canonical_model_fits(con, model_name = "bt-spas-x",
                                      run_years = 2020:2024)

# Canonical pCap one-site fits for tisdale and knights landing
fits <- get_many_canonical_model_fits(con, model_name = "pcap_one_site",
                                      site_selections = c("tisdale", "knights landing"))

# The single canonical pCap all-sites fit
fits <- get_many_canonical_model_fits(con, model_name = "pcap_all_sites")
}
}
