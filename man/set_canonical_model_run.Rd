% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/canonical.R
\name{set_canonical_model_run}
\alias{set_canonical_model_run}
\title{Mark a Model Run as Canonical}
\usage{
set_canonical_model_run(
  con,
  results_name,
  model_run_id,
  set_by,
  site = NULL,
  run_year = NULL,
  site_selection = NULL,
  note = NULL
)
}
\arguments{
\item{con}{A database connection object.}

\item{results_name}{The consolidated model name (e.g. \code{"bt-spas-x"},
\code{"pcap_one_site"}). Must be one of \code{.approved_model_names}.}

\item{model_run_id}{The \code{model_run.id} of the fit to mark canonical.}

\item{set_by}{Who is making this designation (e.g. an email address).}

\item{site}{Optional. Site, for bt-spas-x scopes.}

\item{run_year}{Optional. Run year, for bt-spas-x scopes.}

\item{site_selection}{Optional. Site selection, for pcap_one_site scopes.}

\item{note}{Optional free-text note (e.g. why this version was picked).}
}
\value{
Invisibly returns the number of rows inserted (always 1 on success).
}
\description{
Records \code{model_run_id} as the canonical ("production") fit for a given
model type + scope (site / run_year / site_selection, where applicable).
Does not move, copy, or modify the underlying fit — it only inserts an
audit row pointing at it. Because \code{canonical_model_run} is append-only,
calling this again for the same scope creates a new "current" designation
and preserves the previous one in history (see \code{\link[=get_canonical_history]{get_canonical_history()}}).
}
\examples{
\dontrun{
set_canonical_model_run(
  con = con, results_name = "bt-spas-x", model_run_id = 482,
  set_by = "avizek@flowwest.com", site = "ubc", run_year = 2024,
  note = "Best convergence diagnostics after QA review"
)
}
}
