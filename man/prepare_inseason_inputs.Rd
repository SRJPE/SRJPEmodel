% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/in_season_model.R
\name{prepare_inseason_inputs}
\alias{prepare_inseason_inputs}
\title{Prepare Inputs for In-Season Model}
\usage{
prepare_inseason_inputs(con, stream, site, covariate_effect, autocorrelation)
}
\arguments{
\item{con}{A valid DBI database connection to the SRJPE model run database.}

\item{stream}{The stream identifier (character) for which the model is to be fit.}

\item{site}{The site identifier (character) corresponding to the sampling location.}

\item{covariate_effect}{Logical; if \code{TRUE}, includes covariate effects (e.g., flow) in the model.}

\item{autocorrelation}{Logical; if \code{TRUE}, fits an autocorrelated version of the model (adds lag-1 structure).}
}
\value{
A named list containing:
\describe{
\item{\code{inputs}}{List with \code{data} and \code{inits} used by STAN.}
\item{\code{covariate_effect}}{Logical indicating covariate inclusion.}
\item{\code{autocorrelation}}{Logical indicating autocorrelation.}
\item{\code{model_name}}{Character name of the selected STAN model.}
\item{\code{stream}}{Input stream name.}
\item{\code{site}}{Input site name.}
\item{\code{year_lookup}}{Vector of years used to match indices to results.}
\item{\code{week_lookup}}{Vector of weeks used to match indices to results.}
}
}
\description{
Prepares data inputs required to fit the hierarchical Bayesian in-season STAN model
for estimating fish abundance across multiple years and weeks. This includes transforming
model results, constructing covariate matrices, and assembling input structures required
by the STAN model.
}
\details{
This function extracts juvenile abundance estimates from the model database, aligns them
with a custom week-based calendar, calculates summary statistics (e.g., mean, CV, log-scale SD)
for annual and weekly abundance, processes covariate values (e.g., early season flows),
and assembles the data and initial values for the in-season STAN model.

The function optionally includes covariate effects and autocorrelation, which are passed
as flags and incorporated into the model specification.
}
\seealso{
Other Prepare Model Inputs: 
\code{\link{prepare_P2S_inputs}()},
\code{\link{prepare_abundance_inputs}()},
\code{\link{prepare_pCap_inputs}()},
\code{\link{prepare_stock_recruit_inputs}()},
\code{\link{prepare_survival_inputs}()}
}
\concept{Prepare Model Inputs}
