% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/juvenile_abundance.R
\name{extract_abundance_estimates}
\alias{extract_abundance_estimates}
\title{Extract parameter estimates from the abundance BUGS model object}
\usage{
extract_abundance_estimates(abundance_inputs, model_object)
}
\arguments{
\item{abundance_inputs}{A named list produced by
\code{prepare_abundance_inputs()}, containing at minimum \code{site}, \code{run_year},
\code{model_name}, and \code{weeks_fit}.}

\item{model_object}{A \code{bugs} object produced by
\code{fit_abundance_model_BUGS()}.}
}
\value{
A tidy data frame with one row per parameter–statistic
combination, containing the following columns:
\itemize{
\item \strong{model_name} Character. The abundance model variant
(e.g. \code{"missing_mark_recap"}).
\item \strong{site} Character. The site name.
\item \strong{stream} Character. The stream associated with the site.
\item \strong{run_year} Numeric. The run year.
\item \strong{week_fit} Integer. The Julian week associated with the parameter
(NA for scalars such as \code{Ntot}).
\item \strong{parameter} Character. The parameter name with index notation
removed (e.g. \code{"N"}, \code{"lt_pCap_U"}, \code{"Ntot"}).
\item \strong{statistic} Character. The posterior summary statistic
(e.g. \code{"mean"}, \code{"sd"}, \code{"2.5"}, \code{"50"}, \code{"97.5"}, \code{"Rhat"}).
\item \strong{value} Numeric. The value of the statistic.
\item \strong{srjpedata_version} Character. The version of the \code{SRJPEdata} package
used when producing these estimates.
}
}
\description{
Extract parameter estimates from the abundance BUGS model object
}
\details{
Tidies the summary table from a fitted \code{bugs} object into a long
data frame, attaches week dates and stream names, and records the
\code{SRJPEdata} package version used. The resulting table is in a format
suitable for writing to the database or passing to downstream summaries.
}
